apiVersion: apps/v1
kind: Deployment
metadata:
  name: fintech-webapp
  labels:
    app: fintech-webapp
spec:
  replicas: 2
  selector:
    matchLabels:
      app: fintech-webapp
  template:
    metadata:
      labels:
        app: fintech-webapp
    spec:
      containers:
      - name: fintech-webapp
        image: ghcr.io/open-liontechsolution/fintech-personal-web-app:latest
        ports:
        - containerPort: 3000
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        env:
        # Variables de entorno básicas - las específicas por entorno se definen en los overlays
        - name: DB_HOST
          valueFrom:
            configMapKeyRef:
              name: fintech-webapp-config
              key: db_host
        - name: DB_PORT
          valueFrom:
            configMapKeyRef:
              name: fintech-webapp-config
              key: db_port
        - name: DB_NAME
          valueFrom:
            configMapKeyRef:
              name: fintech-webapp-config
              key: db_name
        - name: DB_USER
          valueFrom:
            secretKeyRef:
              name: fintech-webapp-secrets
              key: db_user
        - name: DB_PASSWORD
          valueFrom:
            secretKeyRef:
              name: fintech-webapp-secrets
              key: db_password
        - name: JWT_SECRET
          valueFrom:
            secretKeyRef:
              name: fintech-webapp-secrets
              key: jwt_secret
        - name: RABBITMQ_URL
          valueFrom:
            secretKeyRef:
              name: fintech-webapp-secrets
              key: rabbitmq_url
        - name: RUN_MIGRATIONS
          value: "true"
        livenessProbe:
          httpGet:
            path: /api/health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5
